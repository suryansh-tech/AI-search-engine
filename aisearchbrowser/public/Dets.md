# About Project

## Pacakages install
- ingnest used for backgroubd LLM jobs

## Cmds 
- nextjs: `npx create-next-app@latest`
- Schadcn ui: `npx shadcn@latest init` with slate theme - new folderadded -> `lib` that has clx and tailwindmerge || also a `components.json` file that has schadcn related informations
- add a button comp: `npx shadcn@latest add button` -> this will add a new components folder
- add sidebar: `npx shadcn@latest add sidebar`
- added tabs switch: `npx shadcn@latest add tabs`
- added dropdown-menu: `npx shadcn@latest add dropdown-menu`

## Functions that implements
- Create SHARED FOLDER -> `shared` file that has all the model for deep searching with agent

### UI
- primary: oklch(0.52 0.0817 211.9);
- --accent: oklch(0.95 0.0067 115.71); for sidebar
- for primary in global.css  chnage the values with oklch formats

- Sidebar is create inside _Compenents here  _ make this folder as normal not a routing folder